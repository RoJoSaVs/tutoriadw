export class VolunteerClass{
    // name: string = "";
    // age: number = 0;
    // country: string = "";
    // type: string = "";

    constructor(public name:string, public age:number, public country:string, public type:string){ }
}